let miString = 'hola mundo';

let miString1 = "hola mundo";

let producto = "manzana";
let descripcionProducto = "La manzana verde es muy rica";
let precio = 2;
let isEnable = true;

const tienda = `
title: ${producto}
description: ${descripcionProducto}
price: ${precio}
isEnable: ${isEnable}
`;

console.log(tienda);